pipeline {
    agent any
    options {

    }
    environment {
        PLAYBOOK_REPO="https://github.com/cmndrsp0ck/glowing-spork.git"
        withCredentials([file(credentialsId: 'backend_tf', variable: 'BACKEND_TF')])
    }

    stages {
        stage('initialize environment') {
            steps {
              pwd
              checkout scm
              git url: "$PLAYBOOK_REPO"

              //git pull "https://github.com/cmndrsp0ck/glowing-spork.git"
            }
        }
/*        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }*/
    }
}
